<!-- ADS -->
<style>
.pf-ad {
  font-family: "Poppins", sans-serif;
  position: relative;
  overflow: hidden;
  border-radius: 6px; /* menos arredondado */
  border: 3px solid #fff; /* borda branca visível */
  box-shadow: 0 4px 20px rgba(0,0,0,0.08);
  background: #fff;
  margin: 20px auto;
  text-align: center;
  transition: transform .2s ease, box-shadow .2s ease;
}

.pf-ad:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(0,0,0,0.1);
}

.pf-ad img {
  width: 100%;
  height: auto;
  display: block;
  border-radius: 4px; /* reduzido para combinar com o novo visual */
  object-fit: cover;
}

.pf-ad a {
  text-decoration: none;
  display: block;
  color: inherit;
}

/* Leaderboard */
.pf-ad[data-type="leaderboard"] {
  max-width: 1200px;
  max-height: 110px;
  border: 3px solid #fff;
  border-radius: 6px;
  box-sizing: border-box;
}

@media (max-width: 600px) {
  .pf-ad[data-type="leaderboard"] {
    max-height: 70px;
  }
}

/* Rectangle & Skyscraper */
.pf-ad[data-type="rectangle"] {
  max-width: 300px;
}

.pf-ad[data-type="skyscraper"] {
  max-width: 160px;
  min-height: 600px;
}

@media (max-width: 900px) {
  .pf-ad[data-type="skyscraper"] {
    display: none;
  }
}

/* Card */
.pf-ad[data-type="card"] {
  max-width: 520px;
  display: flex;
  flex-direction: row;
  border-radius: 6px;
}

.pf-ad[data-type="card"] img {
  width: 40%;
  min-width: 150px;
  height: auto;
  border-radius: 6px 0 0 6px;
}

.pf-ad[data-type="card"] .pf-ad-body {
  padding: 12px;
  text-align: left;
  flex: 1;
}

.pf-ad[data-type="card"] h4 {
  margin: 0 0 6px;
  font-size: 16px;
  color: #222;
}

.pf-ad[data-type="card"] p {
  margin: 0 0 10px;
  font-size: 13px;
  color: #555;
}

.pf-ad[data-type="card"] span {
  font-weight: 600;
  font-size: 13px;
  color: #007bff;
}

@media (max-width: 520px) {
  .pf-ad[data-type="card"] {
    flex-direction: column;
  }
  .pf-ad[data-type="card"] img {
    width: 100%;
    border-radius: 6px 6px 0 0;
  }
}

/* Sticky */
.pf-ad[data-type="sticky"] {
  position: fixed;
  left: 12px;
  right: 12px;
  bottom: 12px;
  z-index: 9999;
  padding: 6px;
  box-shadow: 0 6px 20px rgba(0,0,0,.12);
  display: flex;
  align-items: center;
  gap: 10px;
  background: #fff;
  border-radius: 6px;
}

.pf-ad[data-type="sticky"] img {
  height: 60px;
  width: 100%;
  object-fit: cover;
  border-radius: 4px;
}

.pf-ad[data-type="sticky"] button {
  background: transparent;
  border: 0;
  font-size: 20px;
  margin-left: auto;
  cursor: pointer;
}

@media (min-width: 900px) {
  .pf-ad[data-type="sticky"] {
    display: none;
  }
}

/* Modal */
.pf-modal {
  position: fixed;
  inset: 0;
  display: none;
  align-items: center;
  justify-content: center;
  background: rgba(0,0,0,.6);
  z-index: 10000;
}

.pf-modal[aria-hidden="false"] {
  display: flex;
}

.pf-modal-inner {
  background: #fff;
  padding: 18px;
  border-radius: 6px;
  box-shadow: 0 8px 40px rgba(0,0,0,.3);
  text-align: center;
  position: relative;
}

.pf-modal-inner img {
  width: 320px;
  max-width: 100%;
  border-radius: 6px;
}

.pf-modal-close {
  position: absolute;
  right: 14px;
  top: 14px;
  background: #fff;
  border: 0;
  border-radius: 50%;
  width: 34px;
  height: 34px;
  cursor: pointer;
}
</style>
